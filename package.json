{
  "name": "SimpleWebPage",
  "version": "1.0.0",
  "dependencies": {
    "moment": "^2.16.0"
  },
  "description": "Should be really easy",
  "main": "lambda_function.py",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
